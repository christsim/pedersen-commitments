const PedersenAbtraction = artifacts.require('Pederson');

let instance;

contract('Pedersen', (accounts) => {
  beforeEach(async() => {
    
  });

  it('can add commitments', async() => {
    let instance = await PedersenAbtraction.new();


  });

  
});